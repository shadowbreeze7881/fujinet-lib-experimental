#include <conio.h>
#include "fc_conio.h"

unsigned char toggl_oppos_disp(unsigned char onoff) {
    //return revers(onoff);

    // Ignore, for now.  Many z80 machines does
    //   not have the capability of doing inverse characters.
    return 0;
}